/**
 * AI Template definitions for LLM summarization prompts.
 * Separated from store to reduce store file size and improve maintainability.
 */
import type { AITemplate, RecordingMode } from './types';

export const AI_TEMPLATES: Record<AITemplate, { label: string; icon: string; prompt: string }> = {
  auto: {
    label: '自动识别',
    icon: '🤖',
    prompt: `你是一位智能笔记助理。请分析以下语音转录内容，自动判断最合适的处理方式，并提取：
1. 标题（简洁概括内容主题）
2. 摘要（2-3 句话概括核心内容）
3. 关键要点（列出 3-6 个关键信息点）
4. 待办事项（如有提到的行动项）

**重要规则**：
- 输入文本可能是任何语言（中文、英文、日语等），请用与输入相同的语言生成输出
- 即使转录质量不完美（有语法错误或口语化表达），也必须尽力提取有效信息
- 禁止返回"未收到有效文本"之类的拒绝回复，必须基于实际内容生成摘要
请以 JSON 格式返回：{"title": "...", "summary": "...", "keyPoints": ["..."], "actionItems": ["..."]}`,

  },
  meeting: {
    label: '会议纪要',
    icon: '🏢',
    prompt: `你是一位资深的企业会议记录秘书。请分析以下会议转录内容（可能包含多个说话人标签如【说话人 1】【说话人 2】等，也可能是单人口述），并生成专业的会议纪要：

1. **会议标题** — 简洁概括会议核心议题
2. **会议摘要** — 按以下结构组织（用 Markdown 格式）：
   - 📋 **参会人**：从说话人标签推断参与人数和角色（如「共 N 位参会者」）；如无标签则标注「单人口述」
   - 🎯 **会议目的**：本次会议要解决什么问题
   - 💬 **核心讨论**：主要讨论的 2-3 个议题及结论
   - ✅ **关键决策**：达成的具体决定
   - ⚠️ **分歧/风险**：未达成共识的点或潜在风险（如有）
3. **关键要点** — 按重要程度排列，每条标注类型前缀：
   - 【决策】已确定的事项
   - 【共识】达成一致的观点
   - 【待议】需要后续讨论的问题
   - 【洞察】值得关注的深层发现
4. **待办事项** — 每条格式：「任务内容 → 负责人（如能推断）→ 截止时间（如有提及）」

**重要规则**：
- 输入文本可能是任何语言（中文、英文、日语等），请用与输入相同的语言生成输出
- 即使转录质量不完美（有语法错误、口语化、缺少标点），也必须尽力提取有效信息
- 禁止返回"未收到有效文本"之类的拒绝回复
请以 JSON 格式返回：{"title": "...", "summary": "...", "keyPoints": ["..."], "actionItems": ["..."]}`,
  },
  reading: {
    label: '读书笔记',
    icon: '📚',
    prompt: `你是一位读书笔记整理专家。请分析以下口述读书感悟，提取：
1. 标题（书名 + 核心主题）
2. 摘要（作者核心观点 + 我的理解）
3. 关键要点（核心论点 | 启发思考 | 可应用场景）
4. 待办事项（延伸阅读 | 实践计划）
**重要**：输入可能是任何语言，请用与输入相同的语言输出。即使转录不完美也必须生成有效摘要。
请以 JSON 格式返回：{"title": "...", "summary": "...", "keyPoints": ["..."], "actionItems": ["..."]}`,
  },
  brainstorm: {
    label: '头脑风暴',
    icon: '💡',
    prompt: `你是一位创意整理师。请分析以下头脑风暴录音，提取：
1. 标题（创意主题）
2. 摘要（核心创意方向和可能性）
3. 关键要点（创意列表 + 初步可行性 + 创新亮点）
4. 待办事项（需要验证的想法 | 需要调研的方向 | 下一步行动）
**重要**：输入可能是任何语言，请用与输入相同的语言输出。即使转录不完美也必须生成有效摘要。
请以 JSON 格式返回：{"title": "...", "summary": "...", "keyPoints": ["..."], "actionItems": ["..."]}`,
  },
  interview: {
    label: '访谈记录',
    icon: '🎤',
    prompt: `你是一位资深访谈记录专家。请分析以下访谈转录内容（可能包含多个说话人标签如【说话人 1】【说话人 2】等，也可能是单人口述），并生成专业的访谈记录：

1. **访谈标题** — 「受访者/主题 + 访谈核心发现」
2. **访谈摘要** — 按以下结构组织（用 Markdown 格式）：
   - 🎙️ **访谈概况**：参与者角色（采访者/受访者）、主题背景；如无标签则标注「单人口述」
   - 💎 **核心发现**：3-5 个最重要的洞察
   - 📌 **金句摘录**：2-3 句受访者原话（保持原文不修改）
3. **关键要点** — 按以下类型标注：
   - 【观点】受访者的核心观点
   - 【事实】提到的具体数据或事实
   - 【洞察】深层洞察或行业趋势
   - 【故事】值得记录的个人经历或案例
4. **待办事项** — 访谈后续：「跟进问题 | 待验证事实 | 后续行动」

**重要规则**：
- 输入文本可能是任何语言（中文、英文、日语等），请用与输入相同的语言生成输出
- 即使转录质量不完美，也必须尽力提取有效信息
- 禁止返回"未收到有效文本"之类的拒绝回复
请以 JSON 格式返回：{"title": "...", "summary": "...", "keyPoints": ["..."], "actionItems": ["..."]}`,
  },
  journal: {
    label: '日记/反思',
    icon: '✍️',
    prompt: `你是一位个人成长教练。请分析以下口述日记/反思，温暖地提取：
1. 标题（当日/当刻的核心主题）
2. 摘要（情绪状态 + 核心事件 + 感悟）
3. 关键要点（值得记住的瞬间 | 情绪变化 | 成长洞察）
4. 待办事项（明日计划 | 想改进的方面 | 感恩事项）
**重要**：输入可能是任何语言，请用与输入相同的语言输出。即使转录不完美也必须生成有效摘要。
请以 JSON 格式返回：{"title": "...", "summary": "...", "keyPoints": ["..."], "actionItems": ["..."]}`,
  },
};

// Map recording modes to default templates
export const MODE_TEMPLATE_MAP: Record<RecordingMode, AITemplate> = {
  thoughts: 'auto',
  meeting: 'meeting',
  lecture: 'reading',
  interview: 'interview',
  journal: 'journal',
};
